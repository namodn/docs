# add user to local_domains in /etc/exim/exim.conf
vi /etc/exim/exim.conf

# IMPORTANT - if you don't want every user to 
# get email for this domain, do this :
#
# add domain to /etc/exim-aliases/domains
vi /etc/exim-aliases/domains

# create domain aliases in /etc/exim-aliases/
vi /etc/exim-aliases/domain.com-aliases
